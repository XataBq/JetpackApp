# JetpackApp ✨ (Jetpack Compose / MVVM)

Учебно-портфолийный проект на Jetpack Compose, где я собираю **auth flow** и отрабатываю “production”-подходы: навигационные графы, shared ViewModel, UI-state, события навигации.

---

## ✅ Реализовано
- Navigation Compose с **nested graphs** (auth / main)
- Переходы Registration → Home через UiEvent (однонаправленные события)
- Передача `email` как аргумента route + чтение в `HomeViewModel` через `SavedStateHandle`
- **Shared AuthViewModel**, заскоупленный на `Graph.Auth` (shared state между экранами)
- Общие UI-компоненты вынесены в отдельный пакет (reusable composables)

---

## 🧠 Что я здесь прокачиваю
- State hoisting, корректная работа со state/derivedState
- Разделение UI/логики, устойчивые UI-состояния (sealed state)
- Паттерны навигации и жизненный цикл ViewModel в графах

---

## 🧰 Стек
- Kotlin
- Jetpack Compose
- MVVM
- Navigation Compose

---

## 🗺️ Roadmap
- [ ] DI: Hilt (scopes для графов, modules)
- [ ] Валидация + обработка ошибок (единый UI state)
- [ ] Тесты ViewModel (unit)
